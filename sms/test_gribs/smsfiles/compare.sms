%include <head.h>
%include <tail.h>

ls -l data back

set +e
cmp data back
status=$?
set -e

if [[ $status -ne 0 ]]
then
	/home/ma/emos/bin/linux/compareGribFiles -random -fail -p0 data back
fi

%include <tail.h>

