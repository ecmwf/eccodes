meta ldas_configuration sprintf("%s-%s", modelVersion, targetMarsStream ) : no_copy;

alias mars.configuration = ldas_configuration ;

alias mars.mode = ldasMode ;

if (ldasMode is "nrt"){
 alias mars.anoffset = anoffset ;
}
