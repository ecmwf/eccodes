# Concept satellite
# This brings together all the satellite information to define a channel. This is done via an include.

# include "grib2/satelliteConcepts/satellite.[satelliteSeries].[satelliteNumber].[instrumentType].def"
template_nofail satelliteTemplate "grib2/satelliteConcepts/satellite.[satelliteSeries].[satelliteNumber].[instrumentType].def";
